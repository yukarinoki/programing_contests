#include <cstdio>

int main(){
  int reg[32],mem[2056];
  string buf[100];

  while(EOF!=scanf("%s", buf)){
    switch(buf){
    case mv:
    case add:
    case addi:
    case ld:
    case st:
    case cmp:
    case jeq:

    }
  }



  return 0;
}
